/**
 * The sessions-schedule table style — coral first column, thin dark row rules.

 */
export interface TableProps {
  /** Row data as an array of cell-arrays; first cell of each row renders in coral bold. */
  rows: React.ReactNode[][];
}
