/**
 * Small uppercase eyebrow label above a section headline (e.g. "TREND 1").

 */
export interface SectionLabelProps {
  children?: React.ReactNode;
  tone?: 'coral' | 'violet';
  style?: React.CSSProperties;
}
