/**
 * A thin coral down-arrow connecting stacked cards into a vertical sequence.

 */
export interface ConnectorProps {
  /** Total height in px. */
  length?: number;
  style?: React.CSSProperties;
}
