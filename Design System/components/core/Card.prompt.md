The deck's default content container — flat white, gently rounded, soft neutral shadow, never a border or colored accent stripe.

```jsx
<Card>
  <strong>AI &amp; Business</strong>
  <p>Verstehen wie KI in Unternehmen eingesetzt wird</p>
</Card>
```

Variants: `padding` (px), `hover` (stronger elevation for interactive cards).
